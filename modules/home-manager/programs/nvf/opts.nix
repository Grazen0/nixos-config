{
  programs.nvf.settings.vim = {
    leaderKey = " ";

    options = {
      cursorline = true;
      linebreak = true;
    };

    cursorlineOpt = "both";
    undoFile.enable = true;
    searchCase = "smart";
    useSystemClipboard = true;
    scrollOffset = 4;
  };
}
